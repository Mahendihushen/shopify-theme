<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Custom Theme Demo | {{ shop.name }}</title>
    {{ content_for_header }}
    {{ 'bootstrap.min.css' | asset_url | stylesheet_tag }}
    {{ 'global.css' | asset_url | stylesheet_tag }}
</head>

<body>
    {%- section 'header' -%}
    <main>
        {{ content_for_layout }}
    </main>
    {% section 'footer' %}
</body>
</html>